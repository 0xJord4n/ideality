{
  "binary": "dist/index",
  "standaloneBuild": {
    "maxBytes": 125000000,
    "description": "125 MB maximum for the Linux x64 standalone executable after deferred TUI runtime loading"
  },
  "startup": {
    "args": ["--version"],
    "samples": 5,
    "warmups": 1,
    "maxMedianMs": 500,
    "description": "Median --version startup must remain at or below 500 milliseconds"
  }
}
